<!DOCTYPE html>
<html>
<head>
    <title>Edit Article</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.ckeditor.com/4.16.2/standard/ckeditor.js"></script>
    <script>
        CKEDITOR.replace('content');
    </script>
    <style>
        .bg-burgundy-500 { background-color: #800020; }
        .hover\:bg-burgundy-600:hover { background-color: #660018; }
    </style>
    <script>
        function confirmDelete() {
            return confirm('Are you sure you want to delete this article?');
        }
    </script>
</head>
<body class="bg-gray-200 text-gray-900">
<nav class="bg-gray-800 p-4">
    <a href="/articles" class="text-gray-200 font-bold hover:bg-gray-700 px-3 py-2 rounded">Articles</a>
</nav>
<div class="container mx-auto mt-8">
    <h1 class="text-3xl font-bold mb-4">Edit Article</h1>
    <form action="/articles/{{ article.id }}/edit" method="POST" class="bg-gray-100 p-4 shadow rounded">
        <label for="title" class="block mt-2 font-semibold">Title</label>
        <input type="text" name="title" id="title" value="{{ article.title }}" class="w-full p-2 border border-gray-300 rounded bg-gray-200 text-gray-900" required>
        <label for="content" class="block mt-2 font-semibold">Content</label>
        <textarea name="content" id="content" class="w-full p-2 border border-gray-300 rounded bg-gray-200 text-gray-900" required>{{ article.content }}</textarea>
        <div class="mt-4 flex space-x-2">
            <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Update</button>
        </div>
    </form>
    <form action="/articles/{{ article.id }}/delete" method="POST" class="inline mt-4" onsubmit="return confirmDelete();">
        <button type="submit" class="bg-burgundy-500 text-white px-4 py-2 rounded hover:bg-burgundy-600">Delete</button>
    </form>
    <a href="/articles" class="text-blue-500 hover:text-blue-700 mt-4 block">Back to Articles</a>
</div>
</body>
</html>
